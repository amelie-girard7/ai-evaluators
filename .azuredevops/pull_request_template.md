## Summary

<!-- One sentence: what this PR does and why it matters. -->

## Changes

<!-- Bullet list. Group by area when more than ~5 items. Reference paths, not vague nouns. -->

- 

## Test plan

<!-- How a reviewer can verify the change. Acceptable values include:
     - "Ran `python3 tools/<script>.py` end-to-end; output unchanged."
     - "Visual check in Obsidian + Databricks Repos preview."
     - "manual review only" (for docs/refactors with no runtime behaviour). -->

- 

## Notes

<!-- Optional. Anything the reviewer should know but wouldn't see in the diff:
     decisions you considered and rejected, follow-up work tracked elsewhere,
     known limitations. Delete this section if empty. -->

---

<!-- Reviewer checklist (informational; not a gate):
     [ ] Branch is up to date with main
     [ ] No accidental files (CLAUDE.md, .claude/, outputs/, .DS_Store, etc.)
     [ ] No stray ```mermaid fences (run: python3 tools/render_mermaid.py --check)
     [ ] No unresolved [[wiki-links]] (run: python3 tools/check_home_links.py) -->
